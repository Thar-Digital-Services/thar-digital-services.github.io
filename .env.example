# Google Apps Script Web App URL used by the contact form to send mail.
# Get this URL after deploying contact-form.gs as a Web App
# (see setup instructions at the top of contact-form.gs).
NEXT_PUBLIC_CONTACT_FORM_URL=
